{
  "name": "PendingBreadcrumbLive",
  "type": "object",
  "properties": {
    "driver_id": {
      "type": "string",
      "description": "ID of the driver this live breadcrumb batch belongs to"
    },
    "delivery_start_time": {
      "type": "string",
      "description": "Actual delivery completion timestamp of the previous stop that started this leg"
    },
    "delivery_date": {
      "type": "string",
      "format": "date",
      "description": "Route date for these live breadcrumbs (YYYY-MM-DD)"
    },
    "stop_order": {
      "type": "number",
      "description": "Stop order for reference only"
    },
    "breadcrumbs": {
      "type": "array",
      "description": "Live GPS breadcrumb points captured before consolidation as [lat, lng, timestamp_ms]",
      "items": {
        "type": "array",
        "items": {
          "type": "number"
        },
        "minItems": 2,
        "maxItems": 3
      }
    }
  },
  "required": [
    "driver_id",
    "delivery_start_time",
    "delivery_date"
  ]
}