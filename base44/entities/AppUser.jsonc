{
  "name": "AppUser",
  "type": "object",
  "properties": {
    "company_id": {
      "type": "string",
      "description": "Reference to the Company this user belongs to"
    },
    "user_id": {
      "type": "string",
      "description": "Reference to the authenticated User (their base44 user ID)"
    },
    "app_roles": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "admin",
          "driver",
          "dispatcher",
          "patient"
        ]
      },
      "description": "Array of roles assigned to this user in the app",
      "default": [
        "driver"
      ]
    },
    "user_name": {
      "type": "string",
      "description": "Display name for the user in the app (replaces alias_name)"
    },
    "phone": {
      "type": "string",
      "description": "User's phone number"
    },
    "ETrans_Email": {
      "type": "string",
      "description": "Interac e-Transfer email address for driver payment"
    },
    "home_latitude": {
      "type": "number",
      "description": "User's home address latitude"
    },
    "home_longitude": {
      "type": "number",
      "description": "User's home address longitude"
    },
    "city_id": {
      "type": "string",
      "description": "The user's primary city assignment (legacy - use city_ids for multi-city)"
    },
    "city_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of city IDs that this user is assigned to"
    },
    "store_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of store IDs that this dispatcher is responsible for"
    },
    "square_location_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of SquareLocationConfig IDs assigned to this driver for COD processing"
    },
    "sort_order": {
      "type": "number",
      "description": "A number to manually sort user lists"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive"
      ],
      "default": "active",
      "description": "User account status"
    },
    "driver_status": {
      "type": "string",
      "enum": [
        "off_duty",
        "on_duty",
        "on_break",
        "online"
      ],
      "default": "off_duty",
      "description": "Current driver duty status - controls location tracking. Non-drivers use 'online' to show they are logged in."
    },
    "location_tracking_enabled": {
      "type": "boolean",
      "default": true,
      "description": "Whether the driver has location tracking enabled"
    },
    "current_latitude": {
      "type": "number",
      "description": "Driver's current GPS latitude"
    },
    "current_longitude": {
      "type": "number",
      "description": "Driver's current GPS longitude"
    },
    "location_updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the driver's GPS location was last updated (primary device only)"
    },
    "last_seen_at": {
      "type": "string",
      "format": "date-time",
      "description": "Presence timestamp — written by ALL logged-in devices (primary and non-primary) every 5 minutes. Used to determine if a user has an active session (is at their device). Separate from location_updated_at which is GPS-specific and primary-device-only."
    },
    "last_seen_device": {
      "type": "string",
      "description": "Name of the device that sent the most recent presence heartbeat (for debugging which session is active)"
    },
    "preferred_travel_mode": {
      "type": "string",
      "enum": [
        "driving",
        "cycling",
        "pedestrian"
      ],
      "default": "driving",
      "description": "Preferred travel mode for route optimization and polyline display"
    },
    "pay_rate_per_delivery": {
      "type": "number",
      "description": "Current pay rate per delivery for this driver"
    },
    "extra_km_rate": {
      "type": "number",
      "description": "Current pay rate per extra kilometer driven"
    },
    "extra_km_limit": {
      "type": "number",
      "description": "Minimum Km b4 Extra Km pay rate starts"
    },
    "oversized_item_rate": {
      "type": "number",
      "description": "Extra pay for deliveries marked as oversized"
    },
    "after_hours_rate": {
      "type": "number",
      "description": "Pay rate per after-hours (closed-store) pickup"
    },
    "gst_hst_enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether GST/HST applies to this driver's pay"
    },
    "pay_cycle_type": {
      "type": "string",
      "enum": [
        "weekly",
        "biweekly",
        "semimonthly",
        "monthly"
      ],
      "default": "monthly",
      "description": "Driver's preferred pay cycle type"
    },
    "pay_rate_history": {
      "type": "array",
      "description": "Historical pay rates to preserve accuracy for past pay cycles",
      "items": {
        "type": "object",
        "properties": {
          "effective_date": {
            "type": "string",
            "format": "date",
            "description": "Date when this pay rate became effective"
          },
          "pay_rate_per_delivery": {
            "type": "number",
            "description": "Pay rate per delivery at this time"
          },
          "extra_km_rate": {
            "type": "number",
            "description": "Extra km rate at this time"
          },
          "extra_km_limit": {
            "type": "number",
            "description": "Minimum Km b4 Extra Km pay rate starts"
          },
          "oversized_item_rate": {
            "type": "number",
            "description": "Extra pay for oversized items at this time"
          },
          "after_hours_rate": {
            "type": "number",
            "description": "After-hours pickup rate at this time"
          },
          "pay_cycle_type": {
            "type": "string",
            "enum": [
              "weekly",
              "biweekly",
              "semimonthly",
              "monthly"
            ],
            "description": "Pay cycle type at this time"
          },
          "gst_hst_enabled": {
            "type": "boolean",
            "description": "Whether GST/HST was enabled at this time"
          }
        },
        "required": [
          "effective_date"
        ]
      }
    },
    "deductions": {
      "type": "array",
      "description": "Recurring deductions to apply to this driver's payroll. Each deduction may be scoped to a date range via start_date/end_date (inclusive). A deduction is applied to a pay period when the period's start date falls within that range.",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of the deduction (e.g., 'Uniform Fee', 'Equipment Rental')"
          },
          "amount": {
            "type": "number",
            "description": "Amount of the deduction"
          },
          "start_date": {
            "type": "string",
            "format": "date",
            "description": "Inclusive start date \u2014 deduction applies to pay periods starting on or after this date. Empty = no lower bound (applies to all earlier periods too)."
          },
          "end_date": {
            "type": "string",
            "format": "date",
            "description": "Inclusive end date \u2014 deduction applies to pay periods starting on or before this date. Empty = no upper bound (applies indefinitely until manually removed)."
          }
        },
        "required": [
          "name",
          "amount"
        ]
      }
    },
    "csv_import_mapping": {
      "type": "object",
      "description": "Stores the user's last used CSV to patient field mapping for imports",
      "additionalProperties": true
    }
  },
  "required": [
    "user_id",
    "app_roles"
  ]
}