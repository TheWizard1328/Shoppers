import React, { useState, useEffect } from 'react';

/**
 * OptimizationSpinner — rendering is now handled directly by DashboardView.jsx
 * This component is kept as a no-op so Layout.jsx doesn't break.
 */
export default function OptimizationSpinner() {
  return null;
}
